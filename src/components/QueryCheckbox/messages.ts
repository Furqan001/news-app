/*
 * TargetTypeCheckbox Messages
 *
 * This contains all the text for the TargetTypeCheckbox.
 */

import { defineMessages } from "react-intl";

export const scope = "app.Component.TargetTypeCheckbox";

export default defineMessages({
  label: {
    id: `${scope}.label`,
    defaultMessage: "Target Type",
  },
});
